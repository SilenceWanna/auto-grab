# DrissionPage —— 浏览器自动化与接口请求
# 注:4.1.1.4 稳定版与 Chrome 150 握手不兼容,须用 4.2.0b9+
DrissionPage>=4.2.0b9

# PyYAML —— 读取 YAML 配置
PyYAML>=6.0

# plyer —— 跨平台桌面通知
plyer>=2.1

# requests —— 消息推送（Server酱/钉钉 Webhook 等）
requests>=2.31.0

# tkcalendar —— GUI 日期选择控件(v2.2)
tkcalendar>=1.6
