# 12306 自动抢票脚本 — 工作计划

> 本项目是一个运行在 PC 端的 12306 自动抢票工具，采用**浏览器自动化**方案（DrissionPage 驱动真实浏览器）。
> 本文档为完整的开发路线图与实现说明。

---

## 📌 项目当前状态（2026-07-16）

| 阶段 | 状态 | 备注 |
|------|-----|------|
| 0. 骨架 | ✅ 完成 | 目录、依赖、配置模板、README |
| 1. 登录 | ✅ 完成 | cookies + UAM 会话恢复、checkUser 登录态校验、自动重登、Profile 隔离 |
| 2. 余票查询 | ✅ 完成 | 3375 站码、轮询、席别过滤 |
| 3. 下单占座 | 🧪 干跑通过 | 已进入确认页并选择乘车人/席别；真实提交与排队尚未测试 |
| 4. 通知 | ✅ 完成 | 提示音 + 桌面通知实测通过；Server酱/钉钉推送就位待你填 key |
| 5. 调度健壮性 | 🚧 进行中 | 心跳/退避/重登上限已加，长时压测待做 |
| 6. 打包优化 | ⏸️ 未开始 | 可选 |

**已可用能力**：登录 → 查询余票（能准确定位有票车次和席别）→ 进入确认页 → 选择乘车人和席别。**真实下单保持关闭**，只有明确将 `order.dry_run` 改为 `false` 才会提交。

---

## 一、项目目标

实现一个可在个人电脑上运行的自动化脚本，能够：

1. 自动登录 12306 账号并保持会话。
2. 按用户配置的车次、日期、席别、乘客，轮询查询余票。
3. 一旦出现余票，自动完成选座与提交订单，占座成功。
4. 抢票成功后通过声音 / 桌面通知 / 消息推送提醒用户尽快支付。

> ⚠️ **合规声明**：本项目仅供个人学习与自用，用于代替手动刷新抢票。请勿用于黄牛倒票、高频请求攻击 12306 服务器等违规用途。使用者需对自身行为负责，遵守 12306 的服务条款与相关法律法规。

---

## 二、技术选型

| 层面 | 选型 | 理由 |
|------|------|------|
| 语言 | Python 3.10+ | 生态成熟、开发效率高 |
| 浏览器自动化 | [DrissionPage](https://github.com/g1879/DrissionPage) | 兼具 selenium 的页面控制与 requests 的接口能力，反爬能力强，无需单独管理 driver |
| 配置管理 | YAML | 可读性好，便于用户手动填写车次/乘客信息 |
| 日志 | logging | 标准库，分级记录抢票全过程 |
| 通知 | 声音 + 桌面通知 + 可选 Server酱/钉钉 Webhook | 多渠道确保用户及时收到 |
| 打包（可选） | PyInstaller | 后期可打包为 exe，方便非技术用户使用 |

**为什么选浏览器自动化而非直接调 HTTP 接口**：12306 的接口带有设备指纹、接口签名、滑块验证码等强反爬手段，直接逆向接口极易失效且维护成本高。浏览器方案对页面改版鲁棒性更强，登录与验证码环节可人工介入，更适合个人长期使用。

---

## 三、项目结构

```
auto-grab/
├── README.md               # 使用说明
├── WORKPLAN.md             # 本工作计划
├── requirements.txt        # Python 依赖
├── .gitignore
├── config/
│   └── config.example.yaml # 配置模板（用户复制为 config.yaml 后填写）
├── src/
│   ├── __init__.py
│   ├── main.py             # 程序入口，调度整体抢票流程
│   ├── config.py           # 配置加载与校验
│   ├── login.py            # 登录与会话保持
│   ├── query.py            # 余票查询
│   ├── order.py            # 选座与提交订单
│   ├── notifier.py         # 抢票成功通知
│   └── utils.py            # 通用工具（日志、等待、重试等）
└── logs/                   # 运行日志（git 忽略内容）
```

---

## 四、开发阶段与里程碑

### 阶段 0：项目初始化 ✅（本次交付）
- [x] 确定技术方案与项目结构
- [x] 编写工作计划文档
- [x] 搭建目录骨架与各模块占位实现
- [x] 编写配置模板、依赖清单、README
- [x] 初始化 git 并推送到 GitHub

### 阶段 1：登录模块（`login.py`） ✅
- [x] 启动浏览器，打开 12306 登录页
- [x] 支持账号密码自动填充
- [x] 处理滑块 / 图形验证码（人工介入模式已实现，预留打码平台接口位）
- [x] 登录成功后持久化 cookies，实现会话复用
- [x] 检测登录态失效并自动重登
- **验收**：脚本能稳定登录并进入用户中心页面 ✅
- **额外硬化（实测踩坑后）**：
  - 独立浏览器 Profile 目录，与用户日常 Chrome 完全隔离
  - 使用 9331 附近的常规调试端口（企业环境高位端口会被 EDR 拦截）
  - 关闭时清理浏览器子进程，防止残留堆积
  - Cookies 用 CDP `Network.setCookie` 直接注入（绕开 DrissionPage 4.2.0b9 的 setter 递归 bug）
  - 登录态用查询页同源 `login/checkUser` 接口判断，避免空的 `.login-user` 容器造成假阳性
  - 会话 cookie 的 `expires=-1` 不再作为过期时间注入，并通过 `uamtk → uamauthclient` 恢复 OTN 会话
  - 启动失败自动重试 3 次，二次失败重建 Profile

### 阶段 2：余票查询模块（`query.py`） ✅
- [x] 解析出发地/目的地/日期为 12306 的车站编码（3375 个站码）
- [x] 调用余票查询接口，解析车次列表
- [x] 按配置过滤目标车次与席别
- [x] 实现可配置间隔的轮询（含随机抖动）
- **验收**：能准确输出指定条件下各车次的余票状态 ✅
- **实测确认**：
  - 站名映射从 `station_name.js` 静态资源加载
  - 使用登录会话调用 `leftTicket/query` JSON 接口
  - 等待 Chrome JSON viewer 的 `<pre>` 渲染，避免 `query → queryG` 重定向后的读取竞态
  - 字段索引：车次=3，出发时刻=8，到达时刻=9，二等座=30，一等座=31，商务/特等座=32

### 阶段 3：下单模块（`order.py`） 🧪 完整 dry-run 已通过
- [x] 骨架实现完成：navigate → 预订入口请求 → 勾选乘客 → 选席别 → 提交
- [x] 默认干跑模式（`order.dry_run=true`），零风险测试
- [x] 复用阶段 2 返回的 `secret_str`，调用官方 `submitOrderRequest` 接口，绕开结果表格渲染和偶发假阴性的前置 `checkUser`
- [x] 严格以 `confirmPassenger` URL + 提交按钮判定是否到达确认页
- [x] 修复登录态假阳性：失效 cookies 会进入真实登录流程，不再误走下单
- [x] 修复会话 cookie 的 `expires=-1` 注入问题，并实现 UAM 跨进程会话恢复
- [x] 添加 11 个离线回归测试，覆盖登录态、cookie 注入、预订入口、乘车人、席别和提交结果判定
- [x] 实测进入真实订单确认页并校准乘车人、学生身份成人票提示和席别选择器
- [x] dry-run 校验最终提交所需 token/票串是否齐全，不记录乘车人敏感字段
- [x] 真实模式只点击可见的排队确认框，并严格以 `payOrder/init` 判定成功
- [ ] 将 `dry_run=false` 后验证真实提交、排队和待支付结果（会实际占座，需明确执行）
- **详细诊断与解决记录**：见 [STAGE3_BLOCKER.md](STAGE3_BLOCKER.md)
- **验收**：最终提交前的完整 dry-run 已通过；真实占座验收未执行

### 阶段 4：通知模块（`notifier.py`） 🚧
- [x] 抢票成功播放提示音（Windows winsound 循环 3 声，其他平台降级到终端响铃）
- [x] 发送桌面通知（plyer 跨平台）
- [x] Server酱 推送到微信（sctapi.ftqq.com）
- [x] 钉钉群机器人 Webhook 推送
- [x] 每渠道独立异常隔离；网络请求 8s 超时，防止阻塞抢票主流程
- [x] 自测入口：`python -m src.notifier`（不依赖 12306，独立验证渠道）
- [ ] 端到端联调：将 `order.dry_run=false` 后触发一次真实占座并验证四路通知
- **验收**：占座成功后用户能在多设备及时收到提醒

### 阶段 5：调度与健壮性（`main.py` + `utils.py`） 🚧
- [x] 串联登录→查询→下单→通知全流程
- [x] 统一异常捕获与自动重试，避免单点报错导致整体退出
- [x] 完整日志记录，便于排查
- [x] 心跳日志：每 30 轮打印一次"仍在轮询、共 X 次、运行 M 分 S 秒"
- [x] 连续异常指数退避：≥3 轮出错时把睡眠拉长到最多 60 秒，防止刷屏
- [x] 登录重试上限：连续 3 次重登失败即放弃退出，避免死循环
- [x] `try/finally` 保证浏览器与子进程回收
- [x] Ctrl+C 优雅退出（130 退出码）
- [x] 成功抢票时日志汇报总耗时与尝试次数
- [ ] 长时间空跑压力测试（连续 1 小时无余票场景，验证无内存/进程泄漏）
- [ ] 支持候补下单（可选进阶）
- **验收**：可长时间无人值守稳定运行

### 阶段 6：优化与打包（可选）
- [ ] 多车次 / 多日期并行抢票策略
- [ ] 抢票时段（如放票整点）智能调度
- [ ] PyInstaller 打包为 exe
- [ ] 简易 GUI（可选）

---

## 五、关键技术难点与对策

| 难点 | 对策 |
|------|------|
| 验证码 | 优先登录阶段人工过一次并复用 cookies；预留第三方打码平台接口 |
| 反爬 / 频控 | 轮询间隔加随机抖动；使用真实浏览器指纹；控制请求频率 |
| 页面改版导致选择器失效 | 选择器集中管理、易于维护；关键步骤加显式等待与失败重试 |
| 放票瞬间高并发 | 提前登录占位、预填订单信息，命中后最快路径提交 |
| 长时间运行稳定性 | 全流程异常隔离 + 自动重试 + 心跳日志 |

---

## 六、使用风险提示

- 12306 页面结构与风控策略可能随时变化，脚本需要持续维护。
- 抢票成功不等于支付成功，务必及时人工确认支付。
- 请合理设置轮询频率，避免对 12306 造成压力或触发封控。
