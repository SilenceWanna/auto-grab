# 12306 自动抢票脚本 — 工作计划

> 本项目是一个运行在 PC 端的 12306 自动抢票工具，采用**浏览器自动化**方案（DrissionPage 驱动真实浏览器）。
> 本文档为完整的开发路线图与实现说明。

---

## 📌 项目当前状态（2026-07-17 · v2.1 开发中）

**v1.0 已发布**(git tag `v1.0`,commit `fdecc33`):阶段 0-6 全部实现,dry-run 全流程实测通过。

**v2.0 已发布**(git tag `v2.0`,commit `b85a45b`)新增:
- ✅ 学生票/成人票选择(`trip.ticket_type: adult | student`)
- ✅ 未开票日期友好处理(超出 15 天预售期时静默等待,不产生无效查询与日志噪音)
- ✅ 放票时间自动查询(`schedule.auto: true` + 88 个大站内置放票时刻表)
- ✅ GUI 增强:v2 新字段全部接入,出发地实时提示放票时刻(commit 6adedad)
- ✅ GUI 停止按钮真正生效(stop_event 贯穿主循环与睡眠,commit efe86a3)

**v2.3.2 浏览器 attach 模式**(2026-07-22 紧急,阻塞抢票):
- **背景**:2026-07-22 11:25 尝试抢票前侦察时,DrissionPage 4.2.0b9
  auto-launch Chrome 9331/9332/9333 全部失败,但手动启 Chrome + 调试端口
  9333 完全 OK, 且 DrissionPage 用 `set_address("127.0.0.1:9333")` 能连上。
  说明库的 auto-launch 逻辑在此机器上不稳(与 07-15 症状类似),但 attach
  路径完全可用。
- **修法**:config 新增 `browser.attach_port` 字段。设置后 LoginManager 走
  `set_address` 连已启动的浏览器,不再自启。用户先手动 chrome/msedge
  --remote-debugging-port=9333 挂着,脚本 attach 上去。
- **附加价值**:attach 模式下**cookies 直接复用用户浏览器**,免过滑块最流畅
- **验收**:GUI 里配置 attach_port=9333,查车次/抢票流程全部通

**v2.3.1 热门抢票实战验证**(2026-07-22 阻塞于环境问题,已延期):
- 🎯 目标:用 v2.3 的性能优化真去抢一次热门整点票,验证 2-4 秒 submit 是否真能对抗票池秒空
- 场景(原定 12:00 太原南→北京西 延期,改为 **16:00 北京东→承德南 · 08-06 出行**)
- 走 attach 模式(v2.3.2), dry_run=false, 未支付 30 分钟自动释放
- **1044-1055 侦察时发现两个 bug 需先修**:
  - login `self.page.get(LOGIN_URL)` 后没等 doc_loaded, 25 秒 timeout 都找不到 #J-userName
    (登录页 JS 太重, 需显式等待). 修法:加 `wait.doc_loaded(timeout=15)`
  - GUI 日历默认落在今天, 用户想选未来日期需先导航月份, 容易点错误日期.
    修法:日历打开时如"日期"字段有内容, 定位到那个日期; 否则默认 today+15
- **修完再重新侦察 08-06 车次列表, 挑早班 G 车抢**

**v2.3 下单链路性能优化**(2026-07-22 ✅ 代码 + 实测全部通过):

**实测证据**(2026-07-22 10:35-10:36,北京→天津 C2551,dry-run):
| 次数 | 总耗时 | 关键路径 |
|-----|-------|--------|
| #1 (冷启动) | **9 秒** | 首次 nav init 5s + confirmPassenger 3s + 勾人选席 1s |
| #2-#10 (warm) | **2-4 秒** | init nav 全跳过,只 nav confirmPassenger |

对比昨天(2026-07-21)94 秒,**降幅 90%+**。10 次 submit 中 9 次成功,1 次失败是 UAM 会话过期(与优化无关,4s 后自愈)。

- ✅ 页面预热标志 `_init_page_warm`
- ✅ 跳过重复 `page.get(init) + wait(2)`(每轮省 60+ 秒)
- ✅ `wait.doc_loaded(timeout=5)` 加超时
- ✅ `ele(SEL_SUBMIT_ORDER_BTN, timeout=5)`(原 10s)
- ✅ 单元测试 3 场景 + 真机实测 10 次 submit 均通过

**结论**:第 2 次以后 submit 稳定 2-4 秒,**具备挑战热门票的时间窗口**。
可作为 v2.3 tag 固化。

**v2.2 GUI 增强**(2026-07-17):
- ✅ 日期字段接入 `tkcalendar` 弹历选择器,支持多日追加
- ✅ 新增「🔍 查车次」按钮:GUI 里手动查询当日车次并显示表格
  (车次号/出发/到达/历时/余票摘要),**双击某行自动加入抢票目标车次**
- ✅ 查询在后台线程执行,不阻塞 GUI;与抢票线程互斥,不会同时用同一浏览器

**v2.1 状态**:
- ✅ `_post_json` 兽强化(XHR NetworkError 不再冒到 Python 层导致主循环崩溃,commit 待补)
- ⏸️ 候补下单实现暂停 —— 首次实测发现:12306 现行候补接口 URL 与社区流传的
  `/otn/afterNate/*` 不符,`lcQuery/init` 页面 HTML 不含候补 JS(接口调用藏在
  外部 minified JS 里)。凭"文档"下笔已经踩过阶段 3 的坑,不再重复。
  需要**实机 F12 抓包**拿到真实接口后再重写。已回退候补链代码,保留
  `trip.allow_candidate` 配置字段和文档说明备用。
- ⏸️ v2 版 exe 打包实测(build_exe.py 就绪)

| 阶段 | 状态 | 备注 |
|------|-----|------|
| 0. 骨架 | ✅ 完成 | 目录、依赖、配置模板、README |
| 1. 登录 | ✅ 完成 | cookies + UAM 会话恢复、checkUser 登录态校验、自动重登、Profile 隔离 |
| 2. 余票查询 | ✅ 完成 | 3375 站码、轮询、席别过滤、票种(成人/学生)、预售期识别 |
| 3. 下单占座 | 🧪 干跑通过 | 已进入确认页并选择乘车人/席别;支持学生票弹窗确认;真实提交尚未测试 |
| 4. 通知 | ✅ 完成 | 提示音 + 桌面通知实测通过;Server酱/钉钉推送就位待填 key |
| 5. 调度健壮性 | ✅ 完成 | 心跳/退避/重登上限,Ctrl+C 干净退出,GUI 停止按钮真正响应 |
| 6. 打包优化 | 🚧 v2 增强 | 整点智能调度 + 自动查放票时间 + tkinter GUI(含 v2 新字段) + PyInstaller spec |
| 7. 候补下单 | 🚧 v2.1 | 无票时提交候补订单,让 12306 出票后主动通知 |

**已可用能力**:登录 → 查询余票(含学生票) → 进入确认页 → 选择乘车人和席别。
配置 `schedule.auto=true` 后,填写起点站即自动在放票时刻冲刺;未开票的日期可提前配置,脚本会等到该日期开票才开始查。GUI 里可视化改配置和实时看日志,「停止」按钮秒级响应。**真实下单保持关闭**,只有明确将 `order.dry_run` 改为 `false` 才会提交。

---

## 一、项目目标

实现一个可在个人电脑上运行的自动化脚本，能够：

1. 自动登录 12306 账号并保持会话。
2. 按用户配置的车次、日期、席别、乘客，轮询查询余票。
3. 一旦出现余票，自动完成选座与提交订单，占座成功。
4. 抢票成功后通过声音 / 桌面通知 / 消息推送提醒用户尽快支付。

> ⚠️ **合规声明**：本项目仅供个人学习与自用，用于代替手动刷新抢票。请勿用于黄牛倒票、高频请求攻击 12306 服务器等违规用途。使用者需对自身行为负责，遵守 12306 的服务条款与相关法律法规。

---

## 二、技术选型

| 层面 | 选型 | 理由 |
|------|------|------|
| 语言 | Python 3.10+ | 生态成熟、开发效率高 |
| 浏览器自动化 | [DrissionPage](https://github.com/g1879/DrissionPage) | 兼具 selenium 的页面控制与 requests 的接口能力，反爬能力强，无需单独管理 driver |
| 配置管理 | YAML | 可读性好，便于用户手动填写车次/乘客信息 |
| 日志 | logging | 标准库，分级记录抢票全过程 |
| 通知 | 声音 + 桌面通知 + 可选 Server酱/钉钉 Webhook | 多渠道确保用户及时收到 |
| 打包（可选） | PyInstaller | 后期可打包为 exe，方便非技术用户使用 |

**为什么选浏览器自动化而非直接调 HTTP 接口**：12306 的接口带有设备指纹、接口签名、滑块验证码等强反爬手段，直接逆向接口极易失效且维护成本高。浏览器方案对页面改版鲁棒性更强，登录与验证码环节可人工介入，更适合个人长期使用。

---

## 三、项目结构

```
auto-grab/
├── README.md               # 使用说明
├── WORKPLAN.md             # 本工作计划
├── requirements.txt        # Python 依赖
├── .gitignore
├── config/
│   └── config.example.yaml # 配置模板（用户复制为 config.yaml 后填写）
├── src/
│   ├── __init__.py
│   ├── main.py             # 程序入口，调度整体抢票流程
│   ├── config.py           # 配置加载与校验
│   ├── login.py            # 登录与会话保持
│   ├── query.py            # 余票查询
│   ├── order.py            # 选座与提交订单
│   ├── notifier.py         # 抢票成功通知
│   └── utils.py            # 通用工具（日志、等待、重试等）
└── logs/                   # 运行日志（git 忽略内容）
```

---

## 四、开发阶段与里程碑

### 阶段 0：项目初始化 ✅（本次交付）
- [x] 确定技术方案与项目结构
- [x] 编写工作计划文档
- [x] 搭建目录骨架与各模块占位实现
- [x] 编写配置模板、依赖清单、README
- [x] 初始化 git 并推送到 GitHub

### 阶段 1：登录模块（`login.py`） ✅
- [x] 启动浏览器，打开 12306 登录页
- [x] 支持账号密码自动填充
- [x] 处理滑块 / 图形验证码（人工介入模式已实现，预留打码平台接口位）
- [x] 登录成功后持久化 cookies，实现会话复用
- [x] 检测登录态失效并自动重登
- **验收**：脚本能稳定登录并进入用户中心页面 ✅
- **额外硬化（实测踩坑后）**：
  - 独立浏览器 Profile 目录，与用户日常 Chrome 完全隔离
  - 使用 9331 附近的常规调试端口（企业环境高位端口会被 EDR 拦截）
  - 关闭时清理浏览器子进程，防止残留堆积
  - Cookies 用 CDP `Network.setCookie` 直接注入（绕开 DrissionPage 4.2.0b9 的 setter 递归 bug）
  - 登录态用查询页同源 `login/checkUser` 接口判断，避免空的 `.login-user` 容器造成假阳性
  - 会话 cookie 的 `expires=-1` 不再作为过期时间注入，并通过 `uamtk → uamauthclient` 恢复 OTN 会话
  - 启动失败自动重试 3 次，二次失败重建 Profile

### 阶段 2：余票查询模块（`query.py`） ✅
- [x] 解析出发地/目的地/日期为 12306 的车站编码（3375 个站码）
- [x] 调用余票查询接口，解析车次列表
- [x] 按配置过滤目标车次与席别
- [x] 实现可配置间隔的轮询（含随机抖动）
- [x] **v2:票种支持**(`trip.ticket_type: adult | student`,对应 purpose_codes ADULT/0X00)
- [x] **v2:预售期识别**(超出 15 天预售期的日期静默跳过,不发无效查询)
- **验收**：能准确输出指定条件下各车次的余票状态 ✅
- **实测确认**：
  - 站名映射从 `station_name.js` 静态资源加载
  - 使用登录会话调用 `leftTicket/query` JSON 接口
  - 等待 Chrome JSON viewer 的 `<pre>` 渲染，避免 `query → queryG` 重定向后的读取竞态
  - 字段索引：车次=3，出发时刻=8，到达时刻=9，二等座=30，一等座=31，商务/特等座=32

### 阶段 3：下单模块（`order.py`） 🧪 完整 dry-run 已通过
- [x] 骨架实现完成：navigate → 预订入口请求 → 勾选乘客 → 选席别 → 提交
- [x] 默认干跑模式（`order.dry_run=true`），零风险测试
- [x] 复用阶段 2 返回的 `secret_str`，调用官方 `submitOrderRequest` 接口，绕开结果表格渲染和偶发假阴性的前置 `checkUser`
- [x] 严格以 `confirmPassenger` URL + 提交按钮判定是否到达确认页
- [x] 修复登录态假阳性：失效 cookies 会进入真实登录流程，不再误走下单
- [x] 修复会话 cookie 的 `expires=-1` 注入问题，并实现 UAM 跨进程会话恢复
- [x] 添加 11 个离线回归测试，覆盖登录态、cookie 注入、预订入口、乘车人、席别和提交结果判定
- [x] 实测进入真实订单确认页并校准乘车人、学生身份成人票提示和席别选择器
- [x] dry-run 校验最终提交所需 token/票串是否齐全，不记录乘车人敏感字段
- [x] 真实模式只点击可见的排队确认框，并严格以 `payOrder/init` 判定成功
- [x] **v2:学生票支持**(学生弹窗按 ticket_type 决定点击确认还是取消,submitOrderRequest 也用对应 purpose_codes)
- [ ] 将 `dry_run=false` 后验证真实提交、排队和待支付结果（会实际占座，需明确执行）
- **详细诊断与解决记录**：见 [STAGE3_BLOCKER.md](STAGE3_BLOCKER.md)
- **验收**：最终提交前的完整 dry-run 已通过；真实占座验收未执行

### 阶段 4：通知模块（`notifier.py`） 🚧
- [x] 抢票成功播放提示音（Windows winsound 循环 3 声，其他平台降级到终端响铃）
- [x] 发送桌面通知（plyer 跨平台）
- [x] Server酱 推送到微信（sctapi.ftqq.com）
- [x] 钉钉群机器人 Webhook 推送
- [x] 每渠道独立异常隔离；网络请求 8s 超时，防止阻塞抢票主流程
- [x] 自测入口：`python -m src.notifier`（不依赖 12306，独立验证渠道）
- [ ] 端到端联调：将 `order.dry_run=false` 后触发一次真实占座并验证四路通知
- **验收**：占座成功后用户能在多设备及时收到提醒

### 阶段 5：调度与健壮性（`main.py` + `utils.py`） 🚧
- [x] 串联登录→查询→下单→通知全流程
- [x] 统一异常捕获与自动重试，避免单点报错导致整体退出
- [x] 完整日志记录，便于排查
- [x] 心跳日志：每 30 轮打印一次"仍在轮询、共 X 次、运行 M 分 S 秒"
- [x] 连续异常指数退避：≥3 轮出错时把睡眠拉长到最多 60 秒，防止刷屏
- [x] 登录重试上限：连续 3 次重登失败即放弃退出，避免死循环
- [x] `try/finally` 保证浏览器与子进程回收
- [x] Ctrl+C 优雅退出（130 退出码）
- [x] 成功抢票时日志汇报总耗时与尝试次数
- [ ] 长时间空跑压力测试（连续 1 小时无余票场景，验证无内存/进程泄漏）
- [ ] 支持候补下单（**v2.1 开发中**,见下方阶段 7）
- **验收**：可长时间无人值守稳定运行

### 阶段 6：优化与打包（可选） 🚧
- [x] 放票整点智能调度（config.schedule 段：rush_at / prep / rush_duration / rush_interval）
- [x] **v2:自动查询放票时刻**（`schedule.auto: true`,内置 88 站放票时刻映射,起点站自动匹配）
- [x] tkinter 简易 GUI（`python -m src.gui`）：行程/乘客/开关 + 实时日志窗口
- [x] **v2:GUI 增强** —— 票种下拉、放票时刻手填、自动放票开关、出发地实时提示
- [x] **v2:GUI 停止按钮真正生效** —— stop_event 贯穿主循环与睡眠,秒级响应
- [x] PyInstaller 打包配置（auto-grab.spec + build_exe.py），生成单文件 exe 供非技术用户双击运行
- [ ] 多车次/多日期并行抢票策略（当前已支持串行遍历所有目标组合，真并行 12306 反爬会顶回来，性价比低）
- [ ] 打包 exe 端到端实测（需本地装 PyInstaller）

### 阶段 7:候补下单(v2.1) ⏸️ 暂停
- [x] `_post_json` 兽强化,JS 侧 NetworkError/CORS 错误统一返回失败 dict,不冒到 Python
- [ ] 无票时自动加入 12306 候补队列(`trip.allow_candidate: true`)
- [ ] 走候补接口链:`chooseCandidate` → `passengerInitApi` → `confirmHB`
- **卡点**:2026-07-17 首次实测发现社区流传的 `/otn/afterNate/*` URL 与 12306 现行
  接口不符,`lcQuery/init` 页面 HTML 不含候补 JS(在外部 minified JS 里)。已回退候补
  实现代码,`trip.allow_candidate` 字段保留但当前不启用(即使配 true 也按"暂无余票"处理)。
- **下一步**:需要真实浏览器登录后 F12 Network 面板手动候补一次,把真实 XHR URL/参数
  发我按实际接口重写(与阶段 3 突破 UI 困境同样思路)。

---

## 五、关键技术难点与对策

| 难点 | 对策 |
|------|------|
| 验证码 | 优先登录阶段人工过一次并复用 cookies；预留第三方打码平台接口 |
| 反爬 / 频控 | 轮询间隔加随机抖动；使用真实浏览器指纹；控制请求频率 |
| 页面改版导致选择器失效 | 选择器集中管理、易于维护；关键步骤加显式等待与失败重试 |
| 放票瞬间高并发 | 提前登录占位、预填订单信息，命中后最快路径提交 |
| 长时间运行稳定性 | 全流程异常隔离 + 自动重试 + 心跳日志 |

---

## 六、使用风险提示

- 12306 页面结构与风控策略可能随时变化，脚本需要持续维护。
- 抢票成功不等于支付成功，务必及时人工确认支付。
- 请合理设置轮询频率，避免对 12306 造成压力或触发封控。
